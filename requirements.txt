# Dermatology Benchmark Dependencies
# Python 3.8+

# ===== REQUIRED: Core Workflow =====
# Needed to run the benchmark (run_benchmark.py) and auto-scoring (auto_score.py)
# Generation scripts (generate_patient_profiles.py, generate_dialogues.py) use ONLY standard library

# Google Gemini API (NEW SDK) - for run_benchmark.py and test_api.py
google-genai>=0.2.0

# HTTP requests - for auto_score.py (calls Gemini REST API directly)
requests>=2.28.0

# ===== OPTIONAL: Analysis Scripts Only =====
# Only needed if you want to run the dataset exploration scripts in analysis/ directory
# The core workflow does NOT require these packages

# For exploring datasets (explore_fitzpatrick17k.py, explore_ham10000.py)
pandas>=1.3.0
matplotlib>=3.5.0

# For viewing sample images (explore_ham10000.py)
Pillow>=9.0.0

# For web scraping DermNet patterns (extract_dermnet_patterns.py)
beautifulsoup4>=4.11.0

# ===== OPTIONAL: Development Tools =====
# For better progress bars (uncomment if desired)
# tqdm>=4.64.0

# For Jupyter notebooks (uncomment if desired)
# jupyter>=1.0.0
# ipykernel>=6.0.0
